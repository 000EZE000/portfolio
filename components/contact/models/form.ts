export interface status {
    email: string
    body: string
    confirm: boolean
    feedBack?: string
    statusFeedBack?: boolean
}

export interface Ierror {
    email: string
    body: string
    feedBack?: string
}